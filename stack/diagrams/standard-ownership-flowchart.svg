<svg xmlns="http://www.w3.org/2000/svg" width="100%" viewBox="0 0 680 2400"
     font-family="-apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', 'Segoe UI', sans-serif">
  <style>
    :root {
      --fg: #111827; --fg-muted: #6b7280; --fg-soft: #9ca3af;
      --bg: #ffffff; --divider: #d1d5db;
      --ai-bg: #eff6ff; --ai-stroke: #3b82f6; --ai-text: #1e3a8a;
      --human-bg: #fff7ed; --human-stroke: #f97316; --human-text: #7c2d12;
      --collab-bg: #faf5ff; --collab-stroke: #a855f7; --collab-text: #581c87;
      --phase-pill: #f3f4f6; --phase-pill-stroke: #d1d5db; --phase-text: #374151;
    }
    @media (prefers-color-scheme: dark) {
      :root {
        --fg: #f3f4f6; --fg-muted: #9ca3af; --fg-soft: #6b7280;
        --bg: #0b0f17; --divider: #4b5563;
        --ai-bg: #172554; --ai-stroke: #60a5fa; --ai-text: #dbeafe;
        --human-bg: #431407; --human-stroke: #fb923c; --human-text: #fed7aa;
        --collab-bg: #3b0764; --collab-stroke: #c084fc; --collab-text: #e9d5ff;
        --phase-pill: #1f2937; --phase-pill-stroke: #374151; --phase-text: #d1d5db;
      }
    }
    .bg { fill: var(--bg); }
    .title { font-size: 18px; font-weight: 600; fill: var(--fg); }
    .subtitle { font-size: 12px; fill: var(--fg-muted); }
    .legend-text { font-size: 11px; fill: var(--fg); }
    .phase-divider { stroke: var(--divider); stroke-width: 1; stroke-dasharray: 3,3; }
    .phase-pill { fill: var(--phase-pill); stroke: var(--phase-pill-stroke); stroke-width: 1; }
    .phase-label { font-size: 11px; font-weight: 600; fill: var(--phase-text); letter-spacing: 0.06em; }
    .lane-header { font-size: 10px; font-weight: 700; fill: var(--fg-soft); letter-spacing: 0.12em; }
    .lane-divider { stroke: var(--divider); stroke-width: 1; stroke-dasharray: 2,4; }
    .box-ai     { fill: var(--ai-bg); stroke: var(--ai-stroke); stroke-width: 1.3; }
    .box-human  { fill: var(--human-bg); stroke: var(--human-stroke); stroke-width: 1.3; }
    .box-collab { fill: var(--collab-bg); stroke: var(--collab-stroke); stroke-width: 1.3; }
    .text-ai     { font-size: 12px; font-weight: 500; fill: var(--ai-text); }
    .text-human  { font-size: 12px; font-weight: 500; fill: var(--human-text); }
    .text-collab { font-size: 12px; font-weight: 500; fill: var(--collab-text); }
    .num-ai     { font-size: 10px; font-weight: 700; fill: var(--ai-stroke); }
    .num-human  { font-size: 10px; font-weight: 700; fill: var(--human-stroke); }
    .num-collab { font-size: 10px; font-weight: 700; fill: var(--collab-stroke); }
    .arrow { stroke: var(--fg-soft); stroke-width: 1.3; fill: none; stroke-linecap: round; }
    .arrow-head { fill: var(--fg-soft); stroke: none; }
    .footnote { font-size: 10px; fill: var(--fg-muted); }
  </style>

  <rect class="bg" width="100%" height="100%"/>

  <!-- Title -->
  <text class="title" x="60" y="42">STANDARD.md · Ownership Flow</text>
  <text class="subtitle" x="60" y="62">landing 早做 · 首次 deploy 早触发 · v0 抛光紧跟 deploy · Stripe 独立 · 每 Phase 完都 push</text>

  <!-- Legend -->
  <g>
    <rect class="box-ai" x="60" y="78" width="14" height="14" rx="3"/>
    <text class="legend-text" x="80" y="89">AI 做</text>
    <rect class="box-human" x="130" y="78" width="14" height="14" rx="3"/>
    <text class="legend-text" x="150" y="89">Human 做</text>
    <rect class="box-collab" x="220" y="78" width="14" height="14" rx="3"/>
    <text class="legend-text" x="240" y="89">协作</text>
  </g>

  <!-- Lane headers -->
  <text class="lane-header" x="195" y="114" text-anchor="middle">AI LANE</text>
  <text class="lane-header" x="485" y="114" text-anchor="middle">HUMAN LANE</text>

  <!-- Lane divider full length -->
  <line class="lane-divider" x1="340" y1="124" x2="340" y2="2358"/>

  <!-- ═══════ PHASE 0 label ═══════ -->
  <line class="phase-divider" x1="60" y1="138" x2="250" y2="138"/>
  <rect class="phase-pill" x="252" y="125" width="176" height="26" rx="13"/>
  <text class="phase-label" x="340" y="142" text-anchor="middle">PHASE 0 · BOOTSTRAP</text>
  <line class="phase-divider" x1="430" y1="138" x2="620" y2="138"/>

  <!-- 01 Human: PLAYBOOK_TOKEN -->
  <rect class="box-human" x="360" y="162" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="178">01</text>
  <text class="text-human" x="370" y="190">GitHub Secrets → PLAYBOOK_TOKEN（30s）</text>

  <!-- arrow 01 → Phase 1 label (H → down) -->
  <path class="arrow" d="M 485 194 L 485 208"/>

  <!-- ═══════ PHASE 1 label ═══════ -->
  <line class="phase-divider" x1="60" y1="218" x2="226" y2="218"/>
  <rect class="phase-pill" x="228" y="205" width="224" height="26" rx="13"/>
  <text class="phase-label" x="340" y="222" text-anchor="middle">PHASE 1 · SCAFFOLD + LANDING 壳</text>
  <line class="phase-divider" x1="454" y1="218" x2="620" y2="218"/>

  <!-- arrow from phase 1 label to node 02 (cross H→AI) -->
  <path class="arrow" d="M 485 231 L 485 240 L 195 240 L 195 248"/>
  <polygon class="arrow-head" points="195,254 191,247 199,247"/>

  <!-- 02 AI: Scaffold + cleanup + deps -->
  <rect class="box-ai" x="60" y="254" width="260" height="32" rx="6"/>
  <text class="num-ai"  x="70" y="270">02</text>
  <text class="text-ai" x="70" y="282">Scaffold + §2.5 清理 + deps</text>

  <!-- arrow 02 → 03 (AI → AI vertical) -->
  <path class="arrow" d="M 195 286 L 195 294"/>
  <polygon class="arrow-head" points="195,300 191,293 199,293"/>

  <!-- 03 AI: landing -->
  <rect class="box-ai" x="60" y="300" width="260" height="32" rx="6"/>
  <text class="num-ai"  x="70" y="316">03</text>
  <text class="text-ai" x="70" y="328">写 app/page.tsx landing 壳（先 raw shadcn）</text>

  <!-- arrow 03 → 04 (AI → Human diagonal) -->
  <path class="arrow" d="M 195 332 L 195 340 L 485 340 L 485 348"/>
  <polygon class="arrow-head" points="485,354 481,347 489,347"/>

  <!-- 04 Human: Supabase keys -->
  <rect class="box-human" x="360" y="354" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="370">04</text>
  <text class="text-human" x="370" y="382">Supabase keys → .env.local</text>

  <!-- arrow 04 → Phase 2 label -->
  <path class="arrow" d="M 485 386 L 485 400"/>

  <!-- ═══════ PHASE 2 label ═══════ -->
  <line class="phase-divider" x1="60" y1="410" x2="196" y2="410"/>
  <rect class="phase-pill" x="198" y="397" width="284" height="26" rx="13"/>
  <text class="phase-label" x="340" y="414" text-anchor="middle">PHASE 2 · 首次 VERCEL 部署 🔑</text>
  <line class="phase-divider" x1="484" y1="410" x2="620" y2="410"/>

  <!-- arrow from Phase 2 label to node 05 (H vertical) -->
  <path class="arrow" d="M 485 423 L 485 436"/>
  <polygon class="arrow-head" points="485,442 481,435 489,435"/>

  <!-- 05 Human: Vercel import + paste env + deploy -->
  <rect class="box-human" x="360" y="442" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="458">05</text>
  <text class="text-human" x="370" y="470">Vercel Import + Paste .env + Deploy</text>

  <!-- arrow 05 → 06 (H → H vertical) -->
  <path class="arrow" d="M 485 474 L 485 482"/>
  <polygon class="arrow-head" points="485,488 481,481 489,481"/>

  <!-- 06 Human: runtime test -->
  <rect class="box-human" x="360" y="488" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="504">06</text>
  <text class="text-human" x="370" y="516">打开线上 URL 实测 landing 渲染</text>

  <!-- arrow 06 → Phase 3 label -->
  <path class="arrow" d="M 485 520 L 485 534"/>

  <!-- ═══════ PHASE 3 label (NEW) ═══════ -->
  <line class="phase-divider" x1="60" y1="544" x2="180" y2="544"/>
  <rect class="phase-pill" x="182" y="531" width="316" height="26" rx="13"/>
  <text class="phase-label" x="340" y="548" text-anchor="middle">PHASE 3 · v0 POLISH + TOKEN LOCKIN ✨</text>
  <line class="phase-divider" x1="500" y1="544" x2="620" y2="544"/>

  <!-- arrow Phase 3 label → node 07 (H lane vertical) -->
  <path class="arrow" d="M 485 557 L 485 570"/>
  <polygon class="arrow-head" points="485,576 481,569 489,569"/>

  <!-- 07 Human: v0 brief + paste .tsx -->
  <rect class="box-human" x="360" y="576" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="592">07</text>
  <text class="text-human" x="370" y="604">v0.dev brief + 拿 .tsx 给 AI</text>

  <!-- arrow 07 → 08 (H → AI cross) -->
  <path class="arrow" d="M 485 608 L 485 616 L 195 616 L 195 624"/>
  <polygon class="arrow-head" points="195,630 191,623 199,623"/>

  <!-- 08 AI: 转译 + token 落地 + dark + push -->
  <rect class="box-ai" x="60" y="630" width="260" height="32" rx="6"/>
  <text class="num-ai"  x="70" y="646">08</text>
  <text class="text-ai" x="70" y="658">转译 → globals.css token → push</text>

  <!-- arrow 08 → 09 (AI → Human cross) -->
  <path class="arrow" d="M 195 662 L 195 670 L 485 670 L 485 678"/>
  <polygon class="arrow-head" points="485,684 481,677 489,677"/>

  <!-- 09 Human: 实测 light + dark prod -->
  <rect class="box-human" x="360" y="684" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="700">09</text>
  <text class="text-human" x="370" y="712">浏览器实测 light + dark prod URL</text>

  <!-- arrow 09 → Phase 4 label -->
  <path class="arrow" d="M 485 716 L 485 730"/>

  <!-- ═══════ PHASE 4 label (was 3) ═══════ -->
  <line class="phase-divider" x1="60" y1="740" x2="240" y2="740"/>
  <rect class="phase-pill" x="242" y="727" width="196" height="26" rx="13"/>
  <text class="phase-label" x="340" y="744" text-anchor="middle">PHASE 4 · DB + AUTH</text>
  <line class="phase-divider" x1="440" y1="740" x2="620" y2="740"/>

  <!-- arrow Phase 4 label → node 10 (cross H→AI) -->
  <path class="arrow" d="M 485 753 L 485 762 L 195 762 L 195 770"/>
  <polygon class="arrow-head" points="195,776 191,769 199,769"/>

  <!-- 10 AI: supabase + middleware + auth + schema -->
  <rect class="box-ai" x="60" y="776" width="260" height="32" rx="6"/>
  <text class="num-ai"  x="70" y="792">10</text>
  <text class="text-ai" x="70" y="804">supabase clients + middleware + auth + schema</text>

  <!-- arrow 10 → 11 (AI → Human diagonal) -->
  <path class="arrow" d="M 195 808 L 195 816 L 485 816 L 485 824"/>
  <polygon class="arrow-head" points="485,830 481,823 489,823"/>

  <!-- 11 Human: Supabase Dashboard -->
  <rect class="box-human" x="360" y="830" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="846">11</text>
  <text class="text-human" x="370" y="858">Supabase Dashboard: Email off + OAuth</text>

  <!-- arrow 11 → 12 (H → H vertical) -->
  <path class="arrow" d="M 485 862 L 485 870"/>
  <polygon class="arrow-head" points="485,876 481,869 489,869"/>

  <!-- 12 Human: Google OAuth Client + Supabase keys -->
  <rect class="box-human" x="360" y="876" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="892">12</text>
  <text class="text-human" x="370" y="904">Google OAuth Client → Supabase 粘 keys</text>

  <!-- arrow 12 → 13 (H → AI diagonal) -->
  <path class="arrow" d="M 485 908 L 485 916 L 195 916 L 195 924"/>
  <polygon class="arrow-head" points="195,930 191,923 199,923"/>

  <!-- 13 AI: git push -->
  <rect class="box-ai" x="60" y="930" width="260" height="32" rx="6"/>
  <text class="num-ai"  x="70" y="946">13</text>
  <text class="text-ai" x="70" y="958">git push → Vercel 自动 redeploy</text>

  <!-- arrow 13 → 14 (AI → Human diagonal) -->
  <path class="arrow" d="M 195 962 L 195 970 L 485 970 L 485 978"/>
  <polygon class="arrow-head" points="485,984 481,977 489,977"/>

  <!-- 14 Human: browser auth E2E -->
  <rect class="box-human" x="360" y="984" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="1000">14</text>
  <text class="text-human" x="370" y="1012">浏览器实测 Email + Google + middleware</text>

  <!-- arrow 14 → Phase 5 label -->
  <path class="arrow" d="M 485 1016 L 485 1030"/>

  <!-- ═══════ PHASE 5 label (was 4) ═══════ -->
  <line class="phase-divider" x1="60" y1="1040" x2="196" y2="1040"/>
  <rect class="phase-pill" x="198" y="1027" width="284" height="26" rx="13"/>
  <text class="phase-label" x="340" y="1044" text-anchor="middle">PHASE 5 · STRIPE（可选）</text>
  <line class="phase-divider" x1="484" y1="1040" x2="620" y2="1040"/>

  <!-- arrow from Phase 5 label to node 15 (cross H→AI) -->
  <path class="arrow" d="M 485 1053 L 485 1062 L 195 1062 L 195 1070"/>
  <polygon class="arrow-head" points="195,1076 191,1069 199,1069"/>

  <!-- 15 AI: /api/stripe/{checkout,webhook} -->
  <rect class="box-ai" x="60" y="1076" width="260" height="32" rx="6"/>
  <text class="num-ai"  x="70" y="1092">15</text>
  <text class="text-ai" x="70" y="1104">/api/stripe/{checkout,webhook}</text>

  <!-- arrow 15 → 16 (AI → Human diagonal) -->
  <path class="arrow" d="M 195 1108 L 195 1116 L 485 1116 L 485 1124"/>
  <polygon class="arrow-head" points="485,1130 481,1123 489,1123"/>

  <!-- 16 Human: Stripe Dashboard Product + keys → Vercel env -->
  <rect class="box-human" x="360" y="1130" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="1146">16</text>
  <text class="text-human" x="370" y="1158">Stripe Dashboard: Product + keys → Vercel</text>

  <!-- arrow 16 → 17 (H → H vertical) -->
  <path class="arrow" d="M 485 1162 L 485 1170"/>
  <polygon class="arrow-head" points="485,1176 481,1169 489,1169"/>

  <!-- 17 Human: test mode 付款实测 -->
  <rect class="box-human" x="360" y="1176" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="1192">17</text>
  <text class="text-human" x="370" y="1204">Stripe test mode 付款实测（test card 4242…）</text>

  <!-- arrow 17 → 18 (H → H vertical) -->
  <path class="arrow" d="M 485 1208 L 485 1216"/>
  <polygon class="arrow-head" points="485,1222 481,1215 489,1215"/>

  <!-- 18 Human: 生产 Webhook → secret → redeploy -->
  <rect class="box-human" x="360" y="1222" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="1238">18</text>
  <text class="text-human" x="370" y="1250">生产 Webhook → secret → Vercel redeploy</text>

  <!-- arrow 18 → Phase 6-N label -->
  <path class="arrow" d="M 485 1254 L 485 1268"/>

  <!-- ═══════ PHASE 6-N label (was 5-N) ═══════ -->
  <line class="phase-divider" x1="60" y1="1278" x2="196" y2="1278"/>
  <rect class="phase-pill" x="198" y="1265" width="284" height="26" rx="13"/>
  <text class="phase-label" x="340" y="1282" text-anchor="middle">PHASE 6-N · 业务逻辑（循环）</text>
  <line class="phase-divider" x1="484" y1="1278" x2="620" y2="1278"/>

  <!-- arrow Phase 6-N label → node 19 (cross H→AI) -->
  <path class="arrow" d="M 485 1291 L 485 1300 L 195 1300 L 195 1308"/>
  <polygon class="arrow-head" points="195,1314 191,1307 199,1307"/>

  <!-- 19 AI: 写本 phase 代码 -->
  <rect class="box-ai" x="60" y="1314" width="260" height="32" rx="6"/>
  <text class="num-ai"  x="70" y="1330">19</text>
  <text class="text-ai" x="70" y="1342">写本 phase 代码（一个业务功能）</text>

  <!-- arrow 19 → 20 (AI → AI vertical) -->
  <path class="arrow" d="M 195 1346 L 195 1354"/>
  <polygon class="arrow-head" points="195,1360 191,1353 199,1353"/>

  <!-- 20 AI: git push → Vercel redeploy -->
  <rect class="box-ai" x="60" y="1360" width="260" height="32" rx="6"/>
  <text class="num-ai"  x="70" y="1376">20</text>
  <text class="text-ai" x="70" y="1388">git push → Vercel 自动 redeploy</text>

  <!-- arrow 20 → 21 (AI → Human diagonal) -->
  <path class="arrow" d="M 195 1392 L 195 1400 L 485 1400 L 485 1408"/>
  <polygon class="arrow-head" points="485,1414 481,1407 489,1407"/>

  <!-- 21 Human: 浏览器走本 phase user flow -->
  <rect class="box-human" x="360" y="1414" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="1430">21</text>
  <text class="text-human" x="370" y="1442">浏览器走本 phase user flow</text>

  <!-- Loop arrow: node 21 → back to node 19 (dashed, around right edge) -->
  <path class="arrow" stroke-dasharray="4,3" d="M 620 1430 L 652 1430 L 652 1330 L 324 1330"/>
  <polygon class="arrow-head" points="324,1330 331,1326 331,1334"/>
  <text class="footnote" x="540" y="1324" text-anchor="middle">↻ 每个业务 phase 重复</text>

  <!-- ═══════ POST-MVP A · UAT (creator dogfooding) ═══════ -->
  <line class="phase-divider" x1="60" y1="1490" x2="160" y2="1490"/>
  <rect class="phase-pill" x="162" y="1477" width="356" height="26" rx="13"/>
  <text class="phase-label" x="340" y="1494" text-anchor="middle">POST-MVP A · UAT (CREATOR DOGFOODING §12.A)</text>
  <line class="phase-divider" x1="520" y1="1490" x2="620" y2="1490"/>

  <!-- arrow phase A → node 22 (H lane vertical) -->
  <path class="arrow" d="M 485 1503 L 485 1518"/>
  <polygon class="arrow-head" points="485,1524 481,1517 489,1517"/>

  <!-- 22 Human: 用 MVP 跑 spec 设想的真实场景 -->
  <rect class="box-human" x="360" y="1524" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="1540">22</text>
  <text class="text-human" x="370" y="1552">用 MVP 跑一次 spec 设想的真实场景</text>

  <!-- arrow 22 → 23 (H → AI cross) -->
  <path class="arrow" d="M 485 1556 L 485 1564 L 195 1564 L 195 1572"/>
  <polygon class="arrow-head" points="195,1578 191,1571 199,1571"/>

  <!-- 23 AI: 整理 reflect → issue list → 必修迭代 -->
  <rect class="box-ai" x="60" y="1578" width="260" height="32" rx="6"/>
  <text class="num-ai"  x="70" y="1594">23</text>
  <text class="text-ai" x="70" y="1606">整理 reflect → issue list → 必修迭代</text>

  <!-- arrow 23 → 24 (AI → H cross) -->
  <path class="arrow" d="M 195 1610 L 195 1618 L 485 1618 L 485 1626"/>
  <polygon class="arrow-head" points="485,1632 481,1625 489,1625"/>

  <!-- 24 Human: 重测 → pass / fail decision -->
  <rect class="box-human" x="360" y="1632" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="1648">24</text>
  <text class="text-human" x="370" y="1660">重测 → pass / fail（自己愿不愿继续用？）</text>

  <!-- arrow 24 → next phase B label -->
  <path class="arrow" d="M 485 1664 L 485 1678"/>

  <!-- ═══════ POST-MVP B · CUSTOM DOMAIN ═══════ -->
  <line class="phase-divider" x1="60" y1="1688" x2="170" y2="1688"/>
  <rect class="phase-pill" x="172" y="1675" width="336" height="26" rx="13"/>
  <text class="phase-label" x="340" y="1692" text-anchor="middle">POST-MVP B · CUSTOM DOMAIN (§12.B)</text>
  <line class="phase-divider" x1="510" y1="1688" x2="620" y2="1688"/>

  <!-- arrow phase B → node 25 -->
  <path class="arrow" d="M 485 1701 L 485 1716"/>
  <polygon class="arrow-head" points="485,1722 481,1715 489,1715"/>

  <!-- 25 Human: Vercel Add Domain + Namecheap DNS -->
  <rect class="box-human" x="360" y="1722" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="1738">25</text>
  <text class="text-human" x="370" y="1750">Vercel Add Domain + Namecheap DNS</text>

  <!-- arrow 25 → 26 (H → H vertical) -->
  <path class="arrow" d="M 485 1754 L 485 1762"/>
  <polygon class="arrow-head" points="485,1768 481,1761 489,1761"/>

  <!-- 26 Human: env + Supabase Auth + Stripe webhook -->
  <rect class="box-human" x="360" y="1768" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="1784">26</text>
  <text class="text-human" x="370" y="1796">Vercel env + Supabase Auth + (Stripe webhook)</text>

  <!-- arrow 26 → 27 (H → AI cross) -->
  <path class="arrow" d="M 485 1800 L 485 1808 L 195 1808 L 195 1816"/>
  <polygon class="arrow-head" points="195,1822 191,1815 199,1815"/>

  <!-- 27 AI: grep + push -->
  <rect class="box-ai" x="60" y="1822" width="260" height="32" rx="6"/>
  <text class="num-ai"  x="70" y="1838">27</text>
  <text class="text-ai" x="70" y="1850">grep 硬编码 *.vercel.app → git push</text>

  <!-- arrow 27 → 28 (AI → H cross) -->
  <path class="arrow" d="M 195 1854 L 195 1862 L 485 1862 L 485 1870"/>
  <polygon class="arrow-head" points="485,1876 481,1869 489,1869"/>

  <!-- 28 Human: browser smoke (bare + www) -->
  <rect class="box-human" x="360" y="1876" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="1892">28</text>
  <text class="text-human" x="370" y="1904">浏览器实测 bare + www（OAuth + 路由保护）</text>

  <!-- arrow 28 → next phase C label -->
  <path class="arrow" d="M 485 1908 L 485 1922"/>

  <!-- ═══════ POST-MVP C · SCALE-UP / HARDENING ═══════ -->
  <line class="phase-divider" x1="60" y1="1932" x2="160" y2="1932"/>
  <rect class="phase-pill" x="162" y="1919" width="356" height="26" rx="13"/>
  <text class="phase-label" x="340" y="1936" text-anchor="middle">POST-MVP C · SCALE-UP / HARDENING (§12.C)</text>
  <line class="phase-divider" x1="520" y1="1932" x2="620" y2="1932"/>

  <!-- arrow phase C → node 29 -->
  <path class="arrow" d="M 485 1945 L 485 1960"/>
  <polygon class="arrow-head" points="485,1966 481,1959 489,1959"/>

  <!-- 29 Human: OpenAI cost cap + Sentry/Posthog 注册 -->
  <rect class="box-human" x="360" y="1966" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="1982">29</text>
  <text class="text-human" x="370" y="1994">OpenAI cost cap + Sentry / Posthog 注册</text>

  <!-- arrow 29 → 30 (H → Collab cross-center) -->
  <path class="arrow" d="M 485 1998 L 485 2006 L 340 2006 L 340 2014"/>
  <polygon class="arrow-head" points="340,2020 336,2013 344,2013"/>

  <!-- 30 Collab: Upstash + AI 写 rate-limit + Sentry/Posthog SDK -->
  <rect class="box-collab" x="210" y="2020" width="260" height="32" rx="6"/>
  <text class="num-collab" x="220" y="2036">30</text>
  <text class="text-collab" x="220" y="2048">Upstash + AI 写 rate-limit + Sentry/PH SDK</text>

  <!-- arrow 30 → 31 (Collab → H cross-center) -->
  <path class="arrow" d="M 340 2052 L 340 2060 L 485 2060 L 485 2068"/>
  <polygon class="arrow-head" points="485,2074 481,2067 489,2067"/>

  <!-- 31 Human: dashboard 实测 + 配 alert / funnel -->
  <rect class="box-human" x="360" y="2074" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="2090">31</text>
  <text class="text-human" x="370" y="2102">dashboards 实测 + 配 alert / funnel</text>

  <!-- arrow 31 → next phase D label -->
  <path class="arrow" d="M 485 2106 L 485 2120"/>

  <!-- ═══════ POST-MVP D · STRIPE / MONETIZATION ═══════ -->
  <line class="phase-divider" x1="60" y1="2130" x2="160" y2="2130"/>
  <rect class="phase-pill" x="162" y="2117" width="356" height="26" rx="13"/>
  <text class="phase-label" x="340" y="2134" text-anchor="middle">POST-MVP D · STRIPE / MONETIZATION (§12.D)</text>
  <line class="phase-divider" x1="520" y1="2130" x2="620" y2="2130"/>

  <!-- arrow phase D → node 32 -->
  <path class="arrow" d="M 485 2143 L 485 2158"/>
  <polygon class="arrow-head" points="485,2164 481,2157 489,2157"/>

  <!-- 32 Human: grandfather 策略 + Stripe Dashboard 建 Product -->
  <rect class="box-human" x="360" y="2164" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="2180">32</text>
  <text class="text-human" x="370" y="2192">grandfather 策略 + Stripe 建 Product / keys</text>

  <!-- arrow 32 → 33 (H → Collab cross-center) -->
  <path class="arrow" d="M 485 2196 L 485 2204 L 340 2204 L 340 2212"/>
  <polygon class="arrow-head" points="340,2218 336,2211 344,2211"/>

  <!-- 33 Collab: AI /api/stripe + Human Webhook secret -->
  <rect class="box-collab" x="210" y="2218" width="260" height="32" rx="6"/>
  <text class="num-collab" x="220" y="2234">33</text>
  <text class="text-collab" x="220" y="2246">AI /api/stripe/* + Human 配 webhook secret</text>

  <!-- arrow 33 → 34 (Collab → H cross-center) -->
  <path class="arrow" d="M 340 2250 L 340 2258 L 485 2258 L 485 2266"/>
  <polygon class="arrow-head" points="485,2272 481,2265 489,2265"/>

  <!-- 34 Human: test mode 付款 → live mode -->
  <rect class="box-human" x="360" y="2272" width="260" height="32" rx="6"/>
  <text class="num-human"  x="370" y="2288">34</text>
  <text class="text-human" x="370" y="2300">test mode 付款 → 切 live mode → 通知用户</text>

  <text class="footnote" x="60" y="2380">* 协作紫色节点典型场景：Supabase-only 模式里 AI 写建表 SQL，Human 粘到 SQL Editor；Phase C 的 rate-limit 配置（Human 在 Upstash UI 建 Redis，AI 写 middleware 集成）；Phase D 的 Stripe 接入（AI 写 /api/stripe/*，Human 配 Webhook secret）。</text>
</svg>
